{
  "name": "skin-care-addiction",
  "version": "1.0.0",
  "description": "Skincare product recommendation MCP app",
  "type": "module",
  "scripts": {
    "dev": "mcp-use dev",
    "build": "node build.mjs",
    "start": "node dist/index.js",
    "login": "mcp-use login",
    "deploy": "mcp-use deploy",
    "deploy:cloud": "npm run build && mcp-use deploy"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.97.0",
    "mcp-use": "^1.5.0",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@types/react": "^19.0.0",
    "esbuild": "^0.27.3",
    "react": "^19.0.0",
    "typescript": "^5.5.0"
  }
}
